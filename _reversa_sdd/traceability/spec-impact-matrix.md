# Matriz de Impacto de Especificações (Spec Impact Matrix) — harness-core

> Regenerado pelo Architect em 2026-06-24 (Re-extração após a feature 009-hooks-antigravity)
> Atualização em 2026-06-24 pós-features 007 e 008: bootstrap evolucionário `init`/`upgrade` (f007), e reprodutibilidade com lock file `uv` e consumo dinâmico/glob de exclusões de formatação (f008, resolvendo T4 e T6).
> Atualização em 2026-06-24 pós-feature 009: terceiro driver de entrada (`AntigravityHookBridge`), materializador `.agents/hooks.json` e preenchimento do `AntigravityProfile`; sem dependências novas e sem regressão (110 testes verdes).
> Nível de Documentação: **Completo** · Escala: 🟢 CONFIRMADO · 🟡 INFERIDO
> **Re-extração estrutural de 2026-07-05** (este documento estava congelado desde 2026-06-24/feature 009): novas linhas na matriz — `MigrateService` (f020), `session/close_flow` (f018), `session/resume_context` (f021), `install/session_skills` (f018, substitui a linha que cobria a materialização de commands da f010), `install/claude_settings` (f020); linhas de `InitService`/`BootstrapService`/`SyncService` atualizadas para refletir o split shim/upstream; novas subseções de cobertura para as features 018-021 na seção 2.
> **Reconciliação de 2026-08-11** (pós-features 024-027; a 024 commitada em `5c4433d`, **025/026/027 apenas na working tree nesta data**): linhas `CommandService`/`close_flow`/CLI/`AntigravityHookBridge`/`decisions/gate`/`domain/config`/`HarnessProfile` atualizadas com RN-N48..N55 e a RN-N44 revisada (advisory, f025); novas linhas ✨✨✨✨ para `core/progress` (medidor, f026) e `progress/kanban` (exportador, f027); novas subseções 024-027 na seção 2. Core 2.1.1 → 2.5.0; suíte 372 verde.
> **Reconciliação de 2026-07-15** (pós-MD-0014 e features 022-023): nova linha `decisions/gate`; linhas de `close_flow`/`serializer`/`models`/`config`/CLI/`AntigravityHookBridge`/`SubprocessGitAdapter`/`claude_settings`/`HarnessProfile` atualizadas com RN-N42..N47; menções de T7 "aberto" nas linhas `SyncService`/MCP corrigidas para RESOLVIDO (MD-0013 — o rodapé já dizia, as linhas não); novas subseções MD-0014/022/023 na seção 2.
> **Reconciliação de 2026-08-11-b** (pós-feature 028, **apenas na working tree nesta data**): nenhuma linha nova na matriz — a feature vive em componentes já listados (`DecisionService`, `resume_context`, `InitService`, `config`, CLI, `AntigravityHookBridge`), agora regidos também por RN-N56..N58 e pela RN-N41 revisada; nova subseção 028 na seção 2. Core 2.5.0 → 2.6.0; suíte 389 verde.

Correlaciona os componentes lógicos e adaptadores do `harness-core` com as regras de domínio (RN), as features do ciclo forward, os ADRs/microdecisões e os bugs latentes que tocam cada componente. Severidade = impacto de uma mudança nesse componente sobre o sistema.

---

## 📊 1. Matriz por Componente

| Componente                                                           | Arquivo                               | Regras de Domínio                                                                                                                                              | Feature(s)                  | ADR / MD                                             | Bugs                                                                  | Severidade |
| :------------------------------------------------------------------- | :------------------------------------ | :------------------------------------------------------------------------------------------------------------------------------------------------------------- | :-------------------------- | :--------------------------------------------------- | :-------------------------------------------------------------------- | :--------- |
| **BootstrapService**                                                 | `core/bootstrap/service.py`           | RN-N15 (revisada na f020: não-destrutivo por assinatura, ganchos apontam ao shim)                                                                              | 001, **020**                | 0009, 0020                                           | —                                                                     | MEDIUM     |
| **InitService** ✨                                                   | `core/bootstrap/init_service.py`      | RN-N19 (histórica, **substituída na f020 por RN-N36**), RN-N20 (upgrade, ainda ativo), RN-N26, RN-N58 (guidance write-once por marcador, f028)                 | **007**, **009**, **020**, **028** | 0014, 0016, 0020, 0028                               | —                                                                     | **HIGH**   |
| **FormattingService**                                                | `core/formatting/service.py`          | RN-03, RN-04, RN-05, RN-06, RN-N7, RN-N22, RN-N23, RN-N24                                                                                                      | 002, **008**                | 0002, 0015                                           | T3 resolvido, T4 resolvido (f008)                                     | **HIGH**   |
| **SyncService**                                                      | `core/sync/service.py`                | RN-01, RN-02, RN-N21 (remoção planejada na f020, **desescopada** — ver ADR 0020 Consequências)                                                                 | 007 (mcp/cli)               | 0003, 0014, 0020                                     | T7 RESOLVIDO (MD-0013, fonte única `layout.py`)                       | **HIGH**   |
| **DecisionService**                                                  | `core/decisions/service.py`           | RN-N11, RN-N12, RN-N13, RN-N14; visão compacta derivada na mesma passada (`compile_compact_view`, RN-N56/N57, f028)                                            | **005**, **028**            | 0001, 0012, 0028 / MD-0004, MD-0005, MD-0022         | T1 (resolvido `cf73980`)                                              | **HIGH**   |
| **CommandService**                                                   | `core/commands/service.py`            | RN-07, RN-N1, RN-N3, RN-N4, RN-N5, RN-N31 (revisada na f024: commit condicionado a `versionar_estado`), RN-N32 (versionamento do encerramento, f013), RN-N48 (f024: `execute_command(..., versionar_estado=True)`; MCP mantém `True`, D-04) | **004**, 006, **013**, **024** | 0004, 0010, 0024 / MD-0002, MD-0005, MD-0017         | T2 (resolvido via config, f006)                                       | **HIGH**   |
| **DocumentationService**                                             | `core/documentation/service.py`       | RN-08, RN-09, RN-10                                                                                                                                            | 002                         | 0008                                                 | —                                                                     | MEDIUM     |
| **InstallPromptService** ✨                                          | `core/install/service.py`             | RN-N9, RN-N10                                                                                                                                                  | **003**                     | 0011 / MD-0003                                       | —                                                                     | MEDIUM     |
| **HarnessProfile (Strategy)** ✨                                     | `core/install/harness_profiles.py`    | RN-N10, RN-N5, RN-N26 (f009: `AntigravityProfile` deixa de ser placeholder; escopo por harness migra para `apply_instructions()`); RN-N42 (`ClaudeProfile` sem `PostToolUse`, Stop com `--gate`)                              | **003**, **009**, MD-0014, 022 | 0011, 0016, 0002 (rev. parcial), 0022 / MD-0003, MD-0014 | —                                                                     | MEDIUM     |
| **session/serializer** ✨                                            | `core/session/serializer.py`          | RN-N1, RN-N2, RN-N4, RN-N45 (campos anti-loop opcionais, byte-compat pré-022). Intocado nas f024-027 (a 024 muda fluxo, não dado)                              | **004**, **022**            | 0010, 0022 / MD-0002, MD-0015                        | —                                                                     | **HIGH**   |
| **session/sinks** ✨                                                 | `core/session/sinks.py`               | RN-N5, RN-N6, RN-N8                                                                                                                                            | **004**                     | 0011 / MD-0003                                       | —                                                                     | **HIGH**   |
| **domain/config (`load_config`)**                                    | `core/domain/config.py`               | RN-N11, RN-N18, RN-N40 (versão **2.6.0**); `require_registration` (f022); `ProgressSection` `[progress].file` (f026) com `ProgressKanbanSection` aninhada, opt-in `enabled=False` (f027) — herança sem migração; `DecisionsSection.compact_file`/`compact_index_size` `ge=0` (f028) | **005**, 006, 007, 008, 022, **026**, **027**, **028** | 0012, 0013, 0014, 0015, 0022, 0026, 0027, 0028       | T1 resolvido; T5 fechado (f006); T6 resolvido (f008)                  | **HIGH**   |
| **SessionSection (`[session]`)** ✨                                  | `core/domain/config.py`               | RN-N1 (caminho de sessão por config)                                                                                                                           | **006**                     | 0013 / MD-0005                                       | — (fecha T2)                                                          | **HIGH**   |
| **domain/models**                                                    | `core/domain/models.py`               | RN-N13, RN-N14, RN-N1..N4, RN-N45 (fingerprints do gate zerados no `close_session`)                                                                            | 004, 005, **022**           | 0001, 0010, 0022                                     | —                                                                     | **HIGH**   |
| **LocalFileSystemAdapter**                                           | `adapters/fs/local.py`                | (atomicidade transversal)                                                                                                                                      | —                           | 0006                                                 | —                                                                     | **HIGH**   |
| **SubprocessGitAdapter**                                             | `adapters/git/subprocess.py`          | RN-01, RN-02, RN-07; `list_changed_paths_since` (f022, diff da âncora do gate)                                                                                 | 022                         | 0006, 0022                                           | —                                                                     | MEDIUM     |
| **HostFormatterAdapter**                                             | `adapters/process/formatter.py`       | RN-03, RN-05                                                                                                                                                   | 002, 008                    | 0006, 0015                                           | —                                                                     | MEDIUM     |
| **CLI driver (`main.py`)**                                           | `src/main.py`                         | RN-08, RN-N9, RN-N11, RN-N21; orquestra sinks; `agy-hook` (f009); +`materialize`(012), `migrate`(020); resume + apêndice (021); `decisions --gate` e `cmd --sem-decisao` (f022, RN-N44/N46), identidade grossa no lembrete (f023, RN-N47); flags de consentimento do `cmd encerrar-sessao` (f024, RN-N48); `--gate` advisory em stderr, stdout vazio (f025, RN-N44 revisada); +`progress` (13º subcomando, f026, RN-N51: contrato de exit codes na borda) | 001..027                    | 0007, 0013..0016, 0018..0027                         | T3, T5 e T6 fechados                                                  | **HIGH**   |
| **MCP driver (`server.py`)**                                         | `adapters/mcp/server.py`              | RN-01, RN-N11, RN-N21; expõe 4 tools                                                                                                                           | 006, 007, 008               | 0006, 0013, 0014, 0015                               | T1, T2, T4 resolvidos; T7 RESOLVIDO (MD-0013)                         | **HIGH**   |
| **AntigravityHookBridge** ✨                                         | `adapters/antigravity/hook_bridge.py` | RN-N5, RN-N26, RN-03, RN-02; terceiro driver de entrada (`PreToolUse`/`PostToolUse`/`Stop`), delega a `FormattingService`/`DecisionService`; advisory do gate via `gate_evaluator` (f022, RN-N44; desde a f025 as duas bordas convergiram para a política advisory que este driver sempre teve); `_handle_stop` deriva também a visão compacta (RN-N56, f028) | **009**, 022, 028           | 0016, 0022, 0025, 0028                                     | —                                                                     | **HIGH**   |
| **materialize_hooks_json** ✨                                        | `core/install/antigravity_hooks.py`   | RN-N26, RN-N17 (footprint zero), RN-N19/RN-N20; merge por named-hook `harness`, compartilhado por init e upgrade                                               | **009**                     | 0016                                                 | —                                                                     | MEDIUM     |
| **MigrateService** ✨✨ (NOVO)                                       | `core/migrate/service.py`             | RN-N38 (migração da base instalada), RN-N17 (exceção consciente ao footprint per-projeto)                                                                      | **020**                     | 0020                                                 | —                                                                     | **HIGH**   |
| **session/close_flow** ✨✨ (NOVO)                                   | `core/session/close_flow.py`          | RN-N33 (fluxo único de encerramento), RN-N34/N35 (pré-check restrito ao estado, f019), RN-N31/N32 (versionamento, herdadas; N31 revisada na f024), RN-N44..N47 (3º portão do gate, f022/f023), RN-N48/N49 (f024: oferta consentida no pré-check, tri-estado `versionar_encerramento`, marker `ENCERRAMENTO_NAO_VERSIONADO` com `motivo`) | **018**, **019**, **022**, **023**, **024** | 0018, 0019, 0022, 0023, 0024 / MD-0017               | —                                                                     | **HIGH**   |
| **session/resume_context** ✨✨ (NOVO)                               | `core/session/resume_context.py`      | RN-N41 revisada (apêndice injeta a visão compacta; fallback autoresolvente para o índice, f028)                                                                | **021**, **028**            | 0021, 0028                                           | —                                                                     | MEDIUM     |
| **install/session_skills** ✨✨ (NOVO, substitui `session_commands`) | `core/install/session_skills.py`      | RN-N28/N29 (reescritas: materialização incondicional da skill; superfície encapsulada no perfil)                                                               | **018** (substitui 010/017) | 0018                                                 | —                                                                     | MEDIUM     |
| **install/claude_settings** ✨✨ (NOVO)                              | `core/install/claude_settings.py`     | RN-N39 (merge não-destrutivo por-item), RN-N42 (sem `PostToolUse`, MD-0014; assinatura `decisions` casa `--gate`, f022)                                        | **020**, MD-0014, 022       | 0020, 0002 (rev. parcial), 0022                      | —                                                                     | MEDIUM     |
| **decisions/gate** ✨✨✨ (NOVO)                                     | `core/decisions/gate.py`              | RN-N43 (pendência por sinal físico, avaliação pura), RN-N45 (fingerprints), RN-N47 (dupla identidade fina/grossa). **f025: byte-idêntico** — a despromoção a advisory viveu só na borda (`main.py`); a identidade grossa segue limitando o aviso a um por sessão. Reavaliado em leitura pura pelo medidor (f026, RN-N52) | **022**, **023**            | 0022, 0023, 0025 / MD-0015, MD-0016, MD-0018         | —                                                                     | **HIGH**   |
| **core/progress (medidor)** ✨✨✨✨ (NOVO)                          | `core/progress/{service,stages,render}.py` | RN-N50 (medição é derivação pura, pinada por `fs.writes == []`), RN-N51 (artefato sem valor volátil; write-only-when-changed), RN-N52 (alerta por sinal físico, sem ack; paridade `stages.py` ↔ skill em ponto único) | **026**                     | 0026 / MD-0019                                       | —                                                                     | **HIGH**   |
| **progress/kanban (exportador)** ✨✨✨✨ (NOVO)                     | `core/progress/kanban.py`             | RN-N53 (posse por namespace `category == "harness"`; manuais intocáveis e canal de demandas), RN-N54 (board 100% determinístico, ids `hns:*`, `creation_time` do jsonl), RN-N55 (fluxo unidirecional; jamais toca `vscode-kanban.js`) | **027**                     | 0027 / MD-0020                                       | —                                                                     | MEDIUM     |

> Componentes ✨ são **novos** desde a extração de 2026-06-24 (features 003/004/006; e 009: `AntigravityHookBridge`, `materialize_hooks_json`). Componentes ✨✨ são **novos na reconciliação de 2026-07-05** (features 018/019/020/021). Componentes ✨✨✨ são **novos na reconciliação de 2026-07-15** (features 022/023: `decisions/gate`). Componentes ✨✨✨✨ são **novos na reconciliação de 2026-08-11** (features 026/027: o pacote `core/progress/` — medidor e exportador kanban, as duas projeções da mesma `Medicao` transitória). `SyncService` não tem feature forward dedicada — `sync` é exposto **apenas** via MCP (não há subcomando CLI); permanece ativo apesar do plano da f020 prever sua remoção (ver ADR 0020). O MCP driver, antes sem feature própria, passa a ser tocado pela feature 006 (caminho de sessão por configuração). O `AntigravityHookBridge` é o **terceiro driver de entrada** do hexágono, irmão da CLI e do MCP, e o **único** ponto que conhece o protocolo do Antigravity — nenhum serviço de domínio foi ramificado por harness (RN-N5 reforçada).

---

## 🧩 2. Cobertura por Feature (003 / 004 / 005 / 006)

### Feature 003 — Instalação por Prompt 🟢

- **Componentes:** `InstallPromptService` (orquestra), `HarnessProfile` + `ClaudeProfile`/`GeminiProfile`/`AntigravityProfile` (Strategy), `template.md`; reusa a introspecção do argparse do `DocumentationService`.
- **Regras:** RN-N9 (geração por composição, fonte única), RN-N10 (resolução de perfil fail-fast).
- **Driver:** exposto **apenas pela CLI** (`install-prompt`), não pelo MCP.
- **Rastreabilidade:** ADR 0011 / MD-0003. **Sem bug latente.**

### Feature 004 — Estado de Sessão Unificado 🟢

- **Componentes:** `session/serializer` (round-trip), `session/sinks` (`HookContextSink`/`FileProjectionSink`), `session/errors` (`MalformedSessionStateError`), `CommandService` (consumidor), `SessionState`/`SessionNarrative` (domínio).
- **Regras:** RN-07, RN-N1 (fonte canônica única em `.harness/`), RN-N2 (round-trip), RN-N3 (narrativa preservada), RN-N4 (ausente ≠ malformado), RN-N5 (core não conhece harness), RN-N6 (reinjeção por família), RN-N8 (teto 10000 chars).
- **Driver:** CLI (`cmd resume`/`encerrar-sessao`, hook `SessionStart`) **e** MCP (`session_command`).
- **Rastreabilidade:** ADR 0010 (supera parcialmente 0004) e 0011 / MD-0002, MD-0003.
- **Bug T2 — RESOLVIDO (feature 006):** o MCP apontava para `ESTADO-DA-SESSAO.md` (raiz), divergente da CLI. Desde a feature 006, ambos os drivers leem `session_file` de `config.session.state_file`; não há mais literal de caminho chumbado nem divergência CLI×MCP.

### Feature 005 — Decisões em `.harness/` 🟢

- **Componentes:** `domain/config` (`DecisionsSection` + `load_config`), `DecisionService` (recebe caminhos por parâmetro), os dois drivers (`main.py`, `server.py`) que derivam os caminhos de `load_config().decisions`.
- **Regras:** RN-N11 (caminhos desacoplados via config — watch item W001), RN-N12 (índice derivado), RN-N13 (integridade do grafo), RN-N14 (front-matter obrigatório).
- **Driver:** CLI (`decisions`, hook `Stop`) **e** MCP (`process_decisions`).
- **Rastreabilidade:** ADR 0012 (supera parcialmente 0001) / MD-0004; também ADR 0009 (purge de `claude-config/`, centralização em `.harness/`) / MD-0001.
- **Bug T1 — RESOLVIDO (commit `cf73980`):** via MCP, `load_config` quebrava por import ausente. `server.py` agora importa `from src.core.domain.config import load_config`; `process_decisions` e `session_command` exercem o caminho configurável também via MCP. Não há mais `NameError`.

### Feature 006 — Módulo per-projeto / footprint global zero 🟢

- **Componentes:** `SessionSection` em `domain/config` (caminho de sessão por configuração), `harness.toml` `[session]`, os dois drivers (`main.py`, `server.py`) que leem `config.session.state_file`, o contrato de footprint (`tests/test_footprint.py` + `tests/helpers.py` com `RecordingFileSystem`). Via única de configuração tipada: `load_harness_config`/`import toml` removidos de `main.py` (T5 fechado); `cmd` lê `config.harness.active_harness`.
- **Regras:** RN-N1 (fonte canônica única de sessão, agora por config), BR-MIGRAR-007 (zona protegida `~/.claude`/`~/.agent-memory`, fixada por teste).
- **Driver:** CLI **e** MCP — ambos derivam o caminho de sessão de `config.session.state_file`.
- **Rastreabilidade:** ADR 0013 / MD-0005 (refina MD-0004; reverte a premissa de "config canônica global", afirma o módulo per-projeto autocontido com footprint global zero; NÃO substitui `~/.claude`). A aposentadoria do sync cross-harness (MD-0004) permanece válida.
- **Bugs:** fecha T2 (config) e T5 (via única); herda a resolução de T1/T3 do commit `cf73980`. **Sem bug latente.** Confiança 🟡 só quanto à cobertura do contrato de footprint (cobre os serviços exercitados; é teste, não guard de runtime).

### Feature 007 — Bootstrap e Evolução do Tooling (init/upgrade) 🟢

- **Componentes:** `InitService` (cópia física idempotente, setup de venv, ganchos Git), CLI `main.py` (comandos `init` e `upgrade`), `domain/config` (campos `upstream_path` e `version` em `HarnessSection`), `SyncService` (comparação passiva local de versão local vs upstream).
- **Regras:** RN-N18 (configuração de upstream e versão), RN-N19 (inicialização do alvo), RN-N20 (upgrade não destrutivo), RN-N21 (checagem passiva local no boot).
- **Driver:** CLI (comandos `init`/`upgrade`, checagem passiva) **e** MCP (checagem passiva no boot).
- **Rastreabilidade:** ADR 0014. **Sem bug latente.**

### Feature 008 — Reprodutibilidade e Configurações Dinâmicas de Formatação 🟢

- **Componentes:** `FormattingService` (leitura ativa de `formatting.exclude_paths` e `formatting.opt_out_file` do `HarnessConfig`), venv do core com `requirements.txt` compilado deterministicamente via `uv pip compile` de `requirements.in`, CI/CD workflow em `.github/workflows/ci.yml`.
- **Regras:** RN-N22 (exclusão dinâmica de formatação), RN-N23 (glob patterns via `fnmatch`), RN-N24 (opt-out dinâmico), RN-N25 (lock file e pinning).
- **Driver:** CLI **e** MCP (ambos executam a formatação pelo `FormattingService` parametrizado).
- **Rastreabilidade:** ADR 0015.
- **Bugs:** RESOLVIDO T4 (configurações de formatting inativas) e T6 (ausência de lock file).

### Feature 009 — Ganchos de Ciclo de Vida para o Antigravity 🟢

- **Componentes:** `AntigravityHookBridge` (`adapters/antigravity/hook_bridge.py`) — terceiro driver de entrada que traduz o protocolo de ganchos do Antigravity (`hooks.json` declarativo; stdin/stdout JSON camelCase por evento) e delega aos serviços de domínio; `materialize_hooks_json` (`core/install/antigravity_hooks.py`) — rotina única de escrita por merge do named-hook `harness`, compartilhada por `init` e `upgrade`; `AntigravityProfile` (deixa de ser placeholder: `hooks_block()` emite `.agents/hooks.json` válido e `apply_instructions()` aponta `.agents/hooks.json`); `InitService` (`initialize_project`/`upgrade_project` materializam `.agents/hooks.json` quando `active_harness == "antigravity"`); CLI `main.py` (novo subcomando fino `agy-hook <evento>`, com `agy-hook` na exceção do check passivo de sync e do carregamento global de config).
- **Regras:** RN-N26 (ganchos do Antigravity via `hooks.json` declarativo, traduzido pelo driver de borda; eventos `PreToolUse`/`PostToolUse`/`Stop`; stdin/stdout JSON camelCase). Preservadas e reforçadas: RN-N5 (o core não conhece o harness — confirmado por grep: zero `active_harness` nos serviços de domínio), RN-N9 (4 placeholders no `template.md`), RN-N17 (footprint global zero — `materialize_hooks_json` escreve só dentro do projeto via `FileSystemPort`), RN-03 (não-bloqueio — o adaptador reusa `FormattingService.format_file`, sempre 0, e captura toda exceção), RN-N19/RN-N20 (init/upgrade estendidas com o passo de materialização).
- **Driver:** **terceiro** driver de entrada (`agy-hook` na CLI → `AntigravityHookBridge`), irmão da CLI e do servidor MCP. A reinjeção de estado permanece pelo `FileProjectionSink` (RN-N6 inalterada).
- **Rastreabilidade:** ADR 0016. Removida do `template.md` a nota obsoleta do `SessionStart`/MD-0001 (feature 004 já a fechou). **Sem dependência nova** (só `json`/`sys`/`os` da stdlib). **Sem bug latente.**
- **Confiança 🟡 (premissas de runtime):** a estabilidade do `stepIdx` entre `PreToolUse`/`PostToolUse` e o acesso ao `artifactDirectoryPath` não foram verificáveis sem o runtime real do Antigravity; cobertos por testes de contrato (fixtures), não por integração real. O `command` do `hooks.json` é absoluto (gravado no init/upgrade); mover o repo sem rodar `upgrade` deixa os ganchos apontando para o caminho antigo — dívida mitigada por `upgrade` e documentada no onboarding.

> **Features 010, 012, 013, 014, 015, 016, 017 (não detalhadas em subseção própria nesta reconciliação):** cobrem, respectivamente, a primeira versão de `encerrar-sessao` como command/workflow (010, superada pela skill da 018), o fix de materialização stale via subprocesso (012), o commit de registro do fechamento (013, RN-N31/N32 — linha `CommandService` acima), as ofertas de fim de sessão push/upgrade (014, `session/offers.py` — ver `data-dictionary.md` §8 e ADR relacionada), a reprodutibilidade/config dinâmica (015, já coberta na seção da 008), o encerramento autônomo (016, pré-check de pendência — base da RN-N34 da 019) e a tentativa de caminho via workflow para o Antigravity (017, substituída pela 018). Seu conteúdo integral vive em `domain.md` (§2.7-2.15) e nas ADRs 0014-0018; não são repetidas aqui para não duplicar, mas seus RN permanecem referenciados nas linhas da matriz acima onde aplicável.

### Feature 018 — `encerrar-sessao` como skill versionável (skill como adaptador) 🟢

- **Componentes:** `session/close_flow.py` (`SessionCloseFlow`, fonte única da orquestração — pré-check → gate de narrativa → fechamento via `CommandService` → ofertas); `install/session_skills.py` (`materialize_session_skills`, substitui `install/session_commands.py`); assets da skill (`core/install/assets/skills/encerrar-sessao/{SKILL.md,scripts/}`); `HarnessProfile.skills_dir()` (substitui `session_command_artifact`).
- **Regras:** RN-N28/RN-N29 (reescritas: materialização incondicional da skill; superfície encapsulada no perfil), RN-N33 (nova: fluxo único consumido por CLI e skill).
- **Driver:** CLI (`cmd encerrar-sessao` → `SessionCloseFlow`) **e** a skill materializada (`.claude/skills/` / `.agents/skills/encerrar-sessao/scripts/encerrar_sessao.py` → mesmo `SessionCloseFlow`).
- **Rastreabilidade:** ADR 0018 (substitui ADR 0017). **Sem bug novo** — suíte de 212 testes verde no fechamento da feature, smoke real de `materialize`.

### Feature 019 — Pré-check de pendência restrito ao arquivo de estado 🟢

- **Componentes:** `session/close_flow.pending_work_paths` (filtro estreitado de "diretório inteiro" para "arquivo exato"); `core/domain/layout.SYNC_CACHE_GITIGNORE_ENTRY` (nova entrada de `.gitignore` para o cache de sync, consequência do estreitamento).
- **Regras:** RN-N34 (pendência restrita ao arquivo de estado), RN-N35 (cache de sync explicitamente ignorado).
- **Driver:** CLI (`cmd encerrar-sessao`, via `SessionCloseFlow`) e a skill (mesmo caminho, RN-N33).
- **Rastreabilidade:** ADR 0019. **Achado metodológico, não bug de produto:** o smoke test com git real (não `FakeGit`) revelou que `git status --porcelain` colapsa subdiretórios untracked inteiros numa linha — motivou a correção do filtro.

### Feature 020 — Fonte única de execução + `harness migrate` 🟡 (parcial, ver ADR 0020)

- **Componentes:** `core/bootstrap/shim.py` (`render_shim`, NOVO); `InitService.initialize_project` (reescrito: sem cópia de core/venv); `core/migrate/service.py` (`MigrateService`, NOVO); `core/install/claude_settings.py` (merge por-item, NOVO); `core/bootstrap/service.install_hooks` (não-destrutivo por assinatura); `FileSystemPort.remove_tree` (NOVO no port + adapter); `core/domain/config.CORE_VERSION` (versão canônica única, bump 2.0.0).
- **Regras:** RN-N36 (fonte única no `init`, substitui a RN-N19 histórica), RN-N37 (footprint de escrita preservado), RN-N38 (migração via comando dedicado), RN-N39 (merge não-destrutivo de hooks Claude/git), RN-N40 (versão canônica única).
- **Driver:** CLI (`init`, `migrate`, `bootstrap`); `upgrade`/`SyncService` **permanecem ativos** (não removidos — ver Consequências da ADR 0020).
- **Rastreabilidade:** ADR 0020. **Status parcial confirmado nesta reconciliação:** os itens de remoção de `upgrade_project`/`SyncService`/`version` (roadmap D-05, actions T008/T009/T013/T015/T016) **não foram executados** — desescopados deliberadamente em 2026-07-01 para uma feature futura, ainda não numerada, porque sustentam a `UpgradeOffer` do encerramento (feature 014). `layout.py` mantém os caminhos-candidato legados (`CORE_CONFIG_CANDIDATE_RELPATHS`) que essa remoção eliminaria.

### Feature 021 — Resume ancorado no índice de decisões (Claude-first) 🟢

- **Componentes:** `core/session/resume_context.py` (`build_decisions_appendix`, NOVO, função pura); `core/domain/config.SessionSection.inject_decisions_index` (NOVO campo, default `True`); fiação em `main.py` (ramo `cmd resume`).
- **Regras:** RN-N41 (apêndice do índice de decisões no resume, Claude-first).
- **Driver:** CLI (`cmd resume`, hook `SessionStart` no Claude). Gemini/Antigravity **fora do escopo** desta iteração (decisão explícita, `/reversa-clarify`).
- **Rastreabilidade:** ADR 0021. **Sem bug novo** — suíte 256 verde relatada no fechamento; smoke real dos 4 cenários (índice presente/ausente × flag ligado/desligado). `CommandService.execute_command` (compartilhado com o MCP) permanece intocado — o apêndice é composição pós-chamada, na borda.

### MD-0014 — Aposentadoria do format-on-edit no perfil Claude 🟢

- **Componentes:** `ClaudeProfile.hooks_block()` (não emite mais `PostToolUse → harness format`); `claude_settings._HARNESS_COMMAND_SIGNATURES` (assinatura `"harness format"` removida — itens legados nos alvos ficam preservados como de terceiros, poda ativa descartada); `.reversa/settings.json.snippet` (bloco removido).
- **Regras:** RN-N42. Preservados: `harness format` (comando), `FormattingService`, git pre-commit e o `PostToolUse` do Antigravity.
- **Rastreabilidade:** MD-0014; ADR 0002 marcado "Parcialmente revertido". Suíte 257 verde no fechamento.

### Feature 022 — Gate de registro obrigatório de microdecisões 🟢

- **Componentes:** `core/decisions/gate.py` (NOVO: `evaluate_registration_gate`, `compute_fingerprint`, `GateVerdict`); `GitPort.list_changed_paths_since` + `SubprocessGitAdapter` (NOVO método); `close_flow.py` (3º portão, `render_decisao_pendente_marker`/`conduct_decisao_pendente`, `sem_decisao`); `main.py` (`decisions --gate`, `cmd --sem-decisao`); `AntigravityHookBridge.gate_evaluator` (advisory); `SessionState` (campos anti-loop) + `serializer.py`; `DecisionsSection.require_registration`; `ClaudeProfile` (Stop → `decisions --gate`); skill `encerrar-sessao` 1.3.0.
- **Regras:** RN-N43 (pendência por sinal físico), RN-N44 (enforcement híbrido em três bordas), RN-N45 (anti-loop por fingerprint no estado), RN-N46 (escape auditável).
- **Driver:** CLI (`cmd encerrar-sessao`, `decisions --gate` no hook Stop do Claude) + skill (mesmo `SessionCloseFlow`) + Antigravity (`agy-hook stop`, advisory).
- **Rastreabilidade:** ADR 0022 / MD-0015 (estende MD-0005, relaciona MD-0014). Suíte 293 verde (36 testes novos); smoke real A–F. Core 2.0.1 → 2.1.0.

### Feature 023 — Granularidade do lembrete: dupla identidade anti-loop 🟢

- **Componentes:** `gate.py` (`compute_lembrete_fingerprint`, campo `GateVerdict.fingerprint_lembrete`); `main.py` (ramo `--gate` compara/persiste a identidade grossa); teste-guarda do rearme do portão (`test_close_flow.py::test_gate_portao_rearma_com_trabalho_novo_apos_bloqueio`).
- **Regras:** RN-N47 (lembrete grosso = máx. 1 soft-block/sessão; portão fino rearma com trabalho novo). Transição autoresolvente, sem schema/flag/migração.
- **Rastreabilidade:** ADR 0023 / MD-0016 (estende MD-0015). TDD red→green; suíte 300 verde (7 testes novos); smoke real A–E 9/9. Core 2.1.0 → 2.1.1 (materializadores intocados).


### Feature 024 — Consentimento para escrita no git ao encerrar 🟢

- **Componentes:** `session/close_flow.py` (`run` ganha `com_pendencias` e o tri-estado `versionar_encerramento`; `conduct_commit_pendente` devolve bool; marker `ENCERRAMENTO_NAO_VERSIONADO` com `motivo` esquecimento/recusa, emitido após o sucesso e antes das ofertas); `CommandService.execute_command(..., versionar_estado: bool = True)` (com `False`, fecha o estado no arquivo mas pula `commit_paths`; linha declarativa na narrativa; âncora = HEAD coincide); CLI (`cmd encerrar-sessao` com `--com-commit-encerramento`/`--sem-commit-encerramento`, mutuamente exclusivas, ambas juntas = erro de uso barulhento); skill `encerrar-sessao` 1.4.0 (reage ao marker por `motivo`).
- **Regras:** RN-N48 (toda escrita no git ao encerrar exige consentimento, com default assimétrico por borda: TTY pergunta `[S/n]` default sim; sem TTY, silêncio é recusa), RN-N49 (desfecho não versionado é anunciado, nunca silencioso); RN-N31 revisada (o commit deixa de ser incondicional).
- **Driver:** CLI e skill (mesmo `SessionCloseFlow`); o **MCP mantém `versionar_estado=True`** (D-04 — borda sem TTY para perguntar e sem flag para expressar recusa).
- **Rastreabilidade:** ADR 0024 / MD-0017. Commitada em `5c4433d`. Pendente T028 (propagação manual, 27/28) — feature pausada, não regressão.

### Feature 025 — Aposentadoria do soft-block do Stop 🟢

- **Componentes:** **apenas** o ramo `decisions --gate` do `main.py`: pendência inédita deixa de emitir JSON `{"decision":"block"}` no stdout e vira linha `Aviso:` em stderr; stdout sempre vazio. `gate.py`, `close_flow.py`, serializer, modelos e `.claude/settings.json` **byte-idênticos** (escopo negativo conferido por diff).
- **Regras:** RN-N44 revisada (enforcement colapsa de três políticas para duas: portão duro único no 3º portão do encerramento; advisory nos fins de turno, idêntico nas duas bordas). Mecânica preservada integralmente: avaliação pura, fingerprint grosso persistido antes da emissão (máx. um aviso/sessão), reindexação, fail-open, exit 0.
- **Driver:** hook Stop do Claude (mesmo comando no `settings.json`, comportamento novo propagado pela fonte única, RN-N36); Antigravity já era advisory.
- **Rastreabilidade:** ADR 0025 / MD-0018 (`substitui MD-0016`, `refina MD-0015`). Core 2.2.0 → 2.3.0. **Não commitada nesta data.**

### Feature 026 — Medidor de progresso de entregáveis (read-only) 🟢

- **Componentes:** pacote novo `core/progress/` (`service.py`: `ProgressService.measure()` sobre cinco fontes; `stages.py`: tabela de estágio físico + contagem de checkboxes, paridade com o skill `reversa-requirements`; `render.py`: markdown sem timestamp/caminho absoluto + JSON); `Medicao` transitória (jamais persistida); CLI `progress` (13º subcomando; modos padrão, `--json` com `aferido_em`, `--em-hook` exit 1 só por artefato defasado); `ProgressSection` no config.
- **Regras:** RN-N50 (derivação pura, `fs.writes == []` pinado por teste), RN-N51 (artefato sem valor volátil; contrato de exit codes na borda: fonte ilegível = `Erro de leitura:` stderr exit 2 sem regravar), RN-N52 (alerta persiste enquanto o sinal físico existir — a marca literal "pendência de reconciliação" no regression-watch vira alerta média; gate reavaliado em leitura pura sem persistir fingerprint).
- **Driver:** apenas CLI (`progress`); nenhum hook dispara o medidor por padrão. O exit 3 do medidor original de `comentarios-concursos` **não** foi transplantado (D-03, coerente com a 025).
- **Rastreabilidade:** ADR 0026 / MD-0019 (`relaciona MD-0018`, `refina MD-0013`). Core 2.3.0 → 2.4.0. **Não commitada nesta data.**

### Feature 027 — Exportador kanban com posse por namespace 🟢

- **Componentes:** `core/progress/kanban.py` (único módulo que conhece o schema do fork do vscode-kanban); `Medicao` ganha `board_habilitado`/`demandas` e granularidade de ações (`AcaoProgresso` com IDs reais `T00N`); quinta fonte `_medir_demandas`; `ProgressKanbanSection` opt-in aninhada em `ProgressSection`; board escrito só no modo padrão do `progress`.
- **Regras:** RN-N53 (posse por namespace: card `category == "harness"` recomputado do zero, ids `hns:<feature>[:<T00N>]`/`hns:alerta:<origem>`; manuais preservados byte a byte, fora de `done` viram `Medicao.demandas` — canal de entrada de demandas), RN-N54 (determinismo integral: `creation_time` do primeiro `ts` do `progress.jsonl`, fallback `started-at`, nunca `now()`; idempotência pinada), RN-N55 (fluxo unidirecional: `actions.md` é a fonte de verdade; board ilegível = exit 2 sem escrita; **jamais** cria ou toca `.vscode/vscode-kanban.js`, o arquivo que o fork executa em `workspaces.ts:769`).
- **Driver:** CLI (`progress` modo padrão, com `[progress.kanban] enabled = true`).
- **Rastreabilidade:** ADR 0027 / MD-0020 (`refina MD-0019`; ids ordinais rejeitados no DESCARTADO-e). Core 2.4.0 → 2.5.0. **Não commitada nesta data.** Conferência visual no fork pendente do mantenedor (🟡 nas Observações do regression-watch da 027).

### Feature 028 — Índice de decisões sob demanda (visão compacta) 🟢

- **Componentes:** `DecisionService.compile_compact_view(decisions, output_filepath, index_file, decisions_dir, max_items)` + auxiliares compartilhados `_extract_title`/`_write_if_changed` (`core/decisions/service.py`); `build_decisions_appendix(fs, index_file, enabled, compact_file=None)` com precedência compacta→índice (`session/resume_context.py`); `_ensure_decisions_guidance` como último passo do `initialize_project` (`bootstrap/init_service.py`); `DecisionsSection.compact_file`/`compact_index_size` no config; as duas bordas (CLI `decisions`, com e sem `--gate`, e `_handle_stop` da ponte, com `decisions_compact_file`/`decisions_compact_size` no construtor) derivam a compacta na mesma passada do índice.
- **Regras:** RN-N56 (duas visões, uma passada, duas bordas; write-only-when-changed nas duas escritas; `max_items=0` degrada para cabeçalho + contagem + ponteiros), RN-N57 (compacta é artefato derivado, regenerado por inteiro, nunca fonte — estende RN-N12), RN-N58 (guidance write-once no init, idempotente pelo marcador `<!-- harness:decisoes -->`; o `upgrade` jamais o toca), RN-N41 revisada (resume injeta a compacta; fallback autoresolvente para o índice com aviso em stderr; ambos ausentes → só o estado, exit 0; corte Claude-only preservado).
- **Drivers:** CLI (`decisions`, `cmd resume`, `init`) e ponte Antigravity (`stop`).
- **Rastreabilidade:** ADR 0028 / MD-0022 (`refina MD-0002`, `relaciona MD-0019`). Core 2.5.0 → 2.6.0, suíte 389. **Não commitada nesta data.**

---

## 🛠️ 3. Detalhamento de Impacto Crítico

1. **`session/serializer` + `session/sinks` (HIGH):** o coração da feature 004. Quebra no serializer corrompe a memória de retomada entre boots; quebra no sink impede a reinjeção de contexto. A invariante de round-trip (RN-N2) é a salvaguarda — qualquer mudança exige `test_session.py` e `test_session_sinks.py` verdes.
2. **`DecisionService` + `domain/config` (HIGH):** sustentam o grafo de decisões e o desacoplamento de caminhos (feature 005). Mudança aqui afeta a integridade do índice derivado e o watch item W001. **T1 foi resolvido** no commit `cf73980` (import de `load_config` no MCP); o caminho configurável é hoje exercido tanto pela CLI quanto pelo MCP.
3. **`CommandService` (HIGH):** corrupção do `.harness/estado-da-sessao.md` invalida a retomada do ciclo forward; a âncora Git (RN-07) é o detector de divergência. **T2 foi resolvido** na feature 006 — CLI e MCP convergem para `config.session.state_file`, sem estado paralelo.
4. **`FormattingService` (HIGH):** uma exceção não-blindada travaria a gravação de arquivos do agente (viola RN-03). **T3 foi resolvido** no commit `cf73980` (import de `json` em `main.py`). **T4 foi resolvido** na feature 008 — o serviço consome ativamente as opções do `harness.toml` e suporta exclusão dinâmica por padrões glob via `fnmatch`.
5. **MCP driver `server.py` (HIGH):** antes concentrava T1 e T2, **ambos hoje resolvidos** (`cf73980` e feature 006). As duas tools de decisões e de sessão derivam os caminhos de `load_config`; CLI e MCP são caminhos equivalentes.
6. **`AntigravityHookBridge` (HIGH):** o terceiro driver de entrada da feature 009 e o único ponto que conhece o protocolo do Antigravity (RN-N5). Por ser não-bloqueante por contrato (`_safe` captura toda exceção, loga em `stderr` e emite o stdout-padrão por evento com saída 0), uma quebra aqui não trava o laço do agente, mas perde a formatação/indexação do evento — qualquer mudança exige os testes de payload-fixture (`PreToolUse`/`PostToolUse`/`Stop`) verdes e o teste de footprint de `init --harness antigravity`.
7. **`session/close_flow` (HIGH, novo nesta reconciliação):** fonte única do encerramento (f018) — qualquer regressão aqui afeta simultaneamente a CLI **e** a skill materializada nos dois harnesses (nenhuma lógica duplicada para absorver o impacto). O pré-check de pendência (RN-N34, f019) e o gate de narrativa são os dois pontos que podem **abortar** a transição de estado antes mesmo de chegar ao `CommandService` — mudança aqui exige `test_close_flow.py` verde e smoke com git real (lição da própria f019).
8. **`core/migrate` (HIGH, novo nesta reconciliação):** único componente do sistema com licença deliberada para escrever/remover **fora** do projeto corrente (exceção ao RN-N17). As guardas contra autodestruição (nunca o upstream, nunca autorreferência, `_safe_remove_core` restrito a `harness-core`) são a única rede de segurança contra um `remove_tree` malformado apagar a coisa errada em produção — qualquer mudança aqui exige rodar a suíte **e** um smoke real de `--dry-run` antes de qualquer execução real na base de projetos.
9. **`decisions/gate` (HIGH, novo na reconciliação de 2026-07-15):** o veredito alimenta três bordas simultaneamente (portão do encerramento, hook Stop do Claude, advisory do Antigravity) — uma regressão aqui muda o comportamento das três de uma vez. Os pontos sensíveis são a dupla identidade (grossa ≠ fina: inverter os consumidores reintroduz ou o loop de lembretes da queixa da 023 ou um portão que não rearma) e o fail-open (o gate **nunca** pode travar o agente por erro interno — âncora ilegível vira aviso, não bloqueio). Mudança aqui exige `test_decision_gate.py` + `test_close_flow.py` (incl. o teste-guarda do rearme) + `test_cli.py` verdes.
10. **`core/progress` (HIGH no medidor, novo na reconciliação de 2026-08-11):** as duas invariantes que sustentam a confiança no pacote são a leitura pura da medição (RN-N50 — se `measure()` ganhar qualquer escrita, o medidor vira fonte de estado e o padrão inteiro desmorona; o teste `fs.writes == []` é a tripwire) e a posse por namespace no board (RN-N53/N55 — um bug aqui pode sobrescrever cards manuais do mantenedor, que são canal de demandas, ou criar o arquivo executável do fork). Mudança exige a suíte do pacote verde e, para o kanban, o teste de idempotência byte a byte.

> Situação dos bugs nesta atualização: T1-T6 seguem **totalmente resolvidos** no HEAD (T1/T3 no fix de drivers; T2/T5 na feature 006; T4/T6 na feature 008). A feature 009 não introduziu bug latente. **T7, identificado nesta reconciliação de 2026-07-05, foi RESOLVIDO no mesmo dia (MD-0013):** o cache de sync do servidor MCP usava um nome de arquivo (`sync_cache.json`) divergente do usado pela CLI (`sync-cache.json`), o que fazia o cache do MCP escapar do `.gitignore` do projeto e ser ofertado para commit desde a feature 019. Saneado via fonte única `layout.py:SYNC_CACHE_REL_PATH`, consumida por `main.py`, `close_flow.py` e `server.py`. Ver `architecture.md` §5. As features 024-027 **não introduziram bug latente novo** (suíte 372 verde; smoke reais nos fechamentos das quatro).
