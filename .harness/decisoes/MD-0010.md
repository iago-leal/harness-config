---
id: MD-0010
gancho: caminhos dos regression-watch pré-011 atualizados para o layout .harness/harness-core/ — ponteiros de evidência, não a regra
relacoes:
  - relaciona MD-0005
estado: ativo
---

# MD-0010 — caminhos dos regression-watch pré-011 atualizados para o layout `.harness/`

- **D:** Atualizar, nas **tabelas principais** dos `regression-watch.md` das features 001, 004, 006 e 009, as referências de caminho `harness-core/...` para `.harness/harness-core/...` (7 ocorrências, restritas às colunas Origem, "Como verificar" e ao caminho citado no Sinal de violação). A coluna "Regra esperada após mudança" não foi tocada em nenhuma linha; blocos de histórico datados e observações de 005/011 (que descrevem corretamente o layout antigo) ficaram intactos.
- **PORQUÊ:** A feature 011 relocou o core de `<raiz>/harness-core/` para `.harness/harness-core/`. A re-extração de reconciliação (25/06 13:39) corrigiu os artefatos do `_reversa_sdd/`, mas as tabelas dos watch items antigos continuaram apontando o layout pré-relocação. Não é falha de código — é defasagem documental que corrói o único ativo do mecanismo de regressão: a fiabilidade do sinal. Na rodada completa de regressão (25/06 14:32), o 001/W002 quase virou falso-positivo porque o Sinal de violação ainda citava `harness-core/.venv`; o veredito teve de ser decidido pela essência, e cada rodada futura repetiria o risco. Atualizar o endereço de um arquivo que se mudou conserta a referência sem enfraquecer a regra — preserva a intenção da imutabilidade da tabela (manutenibilidade e mínimo de dívida; erro barulhento sem o ruído de falso-positivo).
- **DESCARTADO:** (1) Não tocar e confiar na leitura por essência a cada rodada — deixa a dívida e o falso-positivo abertos indefinidamente, contra "mínimo de dívida". (2) Migrar as Origens para âncoras de RN (`domain.md#RN-...`), padrão das features 007+ — desacoplaria o watch do path físico (baixo acoplamento), mas é parcialmente inaplicável: 009/W002 e W008 são comandos executáveis (`grep`/`pytest`) e 001/W002 é caminho dentro do Sinal de violação, nenhum ancorável numa RN; forçar geraria heterogeneidade nova ou perda de precisão executável, desproporcional à dívida (Princípio 4). Aproveitável só onde saísse de graça — não houve caso limpo nesta passada. (3) Errata central em `.reversa/` — cria segunda fonte de verdade a cruzar com cada watch, contra coesão.
- **ESTADO:** Aplicado no working tree pelo assistente sob aval explícito do mantenedor; não commitado (a cargo do mantenedor). Edição mecânica e verificável — `grep` confirma zero `harness-core/` sem o prefixo `.harness/` nas tabelas principais, com histórico e observações preservados. Recuperável por git.
